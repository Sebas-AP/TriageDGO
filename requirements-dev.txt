# Dependencias de la fábrica de agentes + fixtures Python (agents/, tests/).
# Ver assets/requirements.txt para dependencias del starter de referencia (RAG real).
pydantic>=2.9.0
jsonschema>=4.20.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
mcp[cli]>=1.0.0,<2.0.0
